<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
             https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="cursoPU" transaction-type="JTA">

        <!-- DataSource registrado en WildFly standalone.xml -->
        <jta-data-source>java:/CursoEstructurasDS</jta-data-source>

        <!-- Entidades del modelo -->
        <class>co.edu.curso.model.ClaseEntity</class>
        <class>co.edu.curso.model.ProgresoEntity</class>

        <properties>
            <property name="hibernate.dialect"
                      value="org.hibernate.dialect.PostgreSQLDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql"   value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.connection.handling_mode"
                      value="DELAYED_ACQUISITION_AND_RELEASE_AFTER_TRANSACTION"/>
        </properties>

    </persistence-unit>

</persistence>
